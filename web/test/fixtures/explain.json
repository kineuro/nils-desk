{
 "hash": "56f5f97518f98ebc92e84c658993c1d5c7c8fbd4062a1dc7c4b1e1ff15207e47",
 "sqlite": "WITH s_scope AS (SELECT q.* FROM (SELECT c.id AS k, NULL AS subj, NULL AS day, NULL AS prec, NULL AS session_k, NULL AS study_k, NULL AS series_k, NULL AS stack_k, c.name AS f_name FROM cohort c) q WHERE (q.f_name IN (?1, ?2))), s_people AS (SELECT su.id AS k, su.id AS subj, NULL AS day, NULL AS prec, NULL AS session_k, NULL AS study_k, NULL AS series_k, NULL AS stack_k, a.k AS cohort_k FROM subject su JOIN cohort_member cm ON cm.subject_id = su.id AND cm.left_at IS NULL JOIN s_scope a ON a.k = cm.cohort_id), s_visits AS MATERIALIZED (SELECT sc.id AS k, su.id AS subj, sc.first AS day, NULL AS prec, sc.id AS session_k, NULL AS study_k, NULL AS series_k, NULL AS stack_k FROM session_cache sc JOIN subject su ON su.id = sc.subject_id LEFT JOIN session_label sl ON sl.session_id = sc.id AND sl.scheme_digest = ?3 JOIN s_people a ON a.k = su.id WHERE sc.window_days = ?4), s_flair AS (SELECT q.* FROM (SELECT st.id AS k, su.id AS subj, COALESCE(sy.date_filled, sy.study_date) AS day, NULL AS prec, scs.session_id AS session_k, sy.id AS study_k, se.id AS series_k, st.id AS stack_k, f.acquisition_type_filled AS f_acquisition_type_filled, f.field_strength_tesla AS f_field_strength_tesla, f.mr_acquisition_type AS f_mr_acquisition_type FROM stack st JOIN series se ON se.id = st.series_id JOIN study sy ON sy.id = se.study_id JOIN subject su ON su.id = sy.subject_id JOIN stack_fingerprint f ON f.stack_id = st.id LEFT JOIN series_mr smr ON smr.series_id = se.id LEFT JOIN session_cache_study scs ON scs.study_id = sy.id AND scs.window_days = ?5 LEFT JOIN session_cache sc ON sc.id = scs.session_id LEFT JOIN session_label sl ON sl.session_id = sc.id AND sl.scheme_digest = ?6 JOIN s_visits a ON a.k = scs.session_id) q WHERE EXISTS (SELECT 1 FROM classification_axis ax WHERE ax.stack_id = q.k AND ax.axis = ?7 AND ax.value = ?8) AND EXISTS (SELECT 1 FROM classification_axis ax WHERE ax.stack_id = q.k AND ax.axis = ?9 AND ax.value = ?10) AND (COALESCE(q.f_acquisition_type_filled, q.f_mr_acquisition_type) = ?11) AND (ABS(q.f_field_strength_tesla - ?12) <= ?13)), s_mp2rage AS (SELECT q.* FROM (SELECT st.id AS k, su.id AS subj, COALESCE(sy.date_filled, sy.study_date) AS day, NULL AS prec, scs.session_id AS session_k, sy.id AS study_k, se.id AS series_k, st.id AS stack_k, f.field_strength_tesla AS f_field_strength_tesla FROM stack st JOIN series se ON se.id = st.series_id JOIN study sy ON sy.id = se.study_id JOIN subject su ON su.id = sy.subject_id JOIN stack_fingerprint f ON f.stack_id = st.id LEFT JOIN series_mr smr ON smr.series_id = se.id LEFT JOIN session_cache_study scs ON scs.study_id = sy.id AND scs.window_days = ?14 LEFT JOIN session_cache sc ON sc.id = scs.session_id LEFT JOIN session_label sl ON sl.session_id = sc.id AND sl.scheme_digest = ?15 JOIN s_visits a ON a.k = scs.session_id) q WHERE EXISTS (SELECT 1 FROM classification_axis ax WHERE ax.stack_id = q.k AND ax.axis = ?16 AND ax.value = ?17) AND (ABS(q.f_field_strength_tesla - ?18) <= ?19)), s_both AS (SELECT q.* FROM (SELECT sc.id AS k, su.id AS subj, sc.first AS day, NULL AS prec, sc.id AS session_k, NULL AS study_k, NULL AS series_k, NULL AS stack_k FROM session_cache sc JOIN subject su ON su.id = sc.subject_id LEFT JOIN session_label sl ON sl.session_id = sc.id AND sl.scheme_digest = ?20 JOIN s_visits x ON x.k = sc.id WHERE sc.window_days = ?21) q WHERE (SELECT COUNT(*) FROM s_flair ch WHERE ch.session_k = q.k) >= 1 AND (SELECT COUNT(*) FROM s_mp2rage ch WHERE ch.session_k = q.k) >= 1) SELECT CAST(COUNT(*) AS INTEGER) AS rows_, CAST(COUNT(DISTINCT o.subj) AS INTEGER) AS subjects_ FROM s_both o",
 "postgres": "WITH s_scope AS (SELECT q.* FROM (SELECT c.id AS k, NULL AS subj, NULL AS day, NULL AS prec, NULL AS session_k, NULL AS study_k, NULL AS series_k, NULL AS stack_k, c.name AS f_name FROM cohort c) q WHERE (q.f_name IN ($1::text, $2::text))), s_people AS (SELECT su.id AS k, su.id AS subj, NULL AS day, NULL AS prec, NULL AS session_k, NULL AS study_k, NULL AS series_k, NULL AS stack_k, a.k AS cohort_k FROM subject su JOIN cohort_member cm ON cm.subject_id = su.id AND cm.left_at IS NULL JOIN s_scope a ON a.k = cm.cohort_id), s_visits AS MATERIALIZED (SELECT sc.id AS k, su.id AS subj, sc.first AS day, NULL AS prec, sc.id AS session_k, NULL AS study_k, NULL AS series_k, NULL AS stack_k FROM session_cache sc JOIN subject su ON su.id = sc.subject_id LEFT JOIN session_label sl ON sl.session_id = sc.id AND sl.scheme_digest = $3::text JOIN s_people a ON a.k = su.id WHERE sc.window_days = $4::bigint), s_flair AS (SELECT q.* FROM (SELECT st.id AS k, su.id AS subj, COALESCE(sy.date_filled, sy.study_date) AS day, NULL AS prec, scs.session_id AS session_k, sy.id AS study_k, se.id AS series_k, st.id AS stack_k, f.acquisition_type_filled AS f_acquisition_type_filled, f.field_strength_tesla AS f_field_strength_tesla, f.mr_acquisition_type AS f_mr_acquisition_type FROM stack st JOIN series se ON se.id = st.series_id JOIN study sy ON sy.id = se.study_id JOIN subject su ON su.id = sy.subject_id JOIN stack_fingerprint f ON f.stack_id = st.id LEFT JOIN series_mr smr ON smr.series_id = se.id LEFT JOIN session_cache_study scs ON scs.study_id = sy.id AND scs.window_days = $5::bigint LEFT JOIN session_cache sc ON sc.id = scs.session_id LEFT JOIN session_label sl ON sl.session_id = sc.id AND sl.scheme_digest = $6::text JOIN s_visits a ON a.k = scs.session_id) q WHERE EXISTS (SELECT 1 FROM classification_axis ax WHERE ax.stack_id = q.k AND ax.axis = $7::text AND ax.value = $8::text) AND EXISTS (SELECT 1 FROM classification_axis ax WHERE ax.stack_id = q.k AND ax.axis = $9::text AND ax.value = $10::text) AND (COALESCE(q.f_acquisition_type_filled, q.f_mr_acquisition_type) = $11::text) AND (ABS(q.f_field_strength_tesla - $12::double precision) <= $13::double precision)), s_mp2rage AS (SELECT q.* FROM (SELECT st.id AS k, su.id AS subj, COALESCE(sy.date_filled, sy.study_date) AS day, NULL AS prec, scs.session_id AS session_k, sy.id AS study_k, se.id AS series_k, st.id AS stack_k, f.field_strength_tesla AS f_field_strength_tesla FROM stack st JOIN series se ON se.id = st.series_id JOIN study sy ON sy.id = se.study_id JOIN subject su ON su.id = sy.subject_id JOIN stack_fingerprint f ON f.stack_id = st.id LEFT JOIN series_mr smr ON smr.series_id = se.id LEFT JOIN session_cache_study scs ON scs.study_id = sy.id AND scs.window_days = $14::bigint LEFT JOIN session_cache sc ON sc.id = scs.session_id LEFT JOIN session_label sl ON sl.session_id = sc.id AND sl.scheme_digest = $15::text JOIN s_visits a ON a.k = scs.session_id) q WHERE EXISTS (SELECT 1 FROM classification_axis ax WHERE ax.stack_id = q.k AND ax.axis = $16::text AND ax.value = $17::text) AND (ABS(q.f_field_strength_tesla - $18::double precision) <= $19::double precision)), s_both AS (SELECT q.* FROM (SELECT sc.id AS k, su.id AS subj, sc.first AS day, NULL AS prec, sc.id AS session_k, NULL AS study_k, NULL AS series_k, NULL AS stack_k FROM session_cache sc JOIN subject su ON su.id = sc.subject_id LEFT JOIN session_label sl ON sl.session_id = sc.id AND sl.scheme_digest = $20::text JOIN s_visits x ON x.k = sc.id WHERE sc.window_days = $21::bigint) q WHERE (SELECT COUNT(*) FROM s_flair ch WHERE ch.session_k = q.k) >= 1 AND (SELECT COUNT(*) FROM s_mp2rage ch WHERE ch.session_k = q.k) >= 1) SELECT CAST(COUNT(*) AS BIGINT) AS rows_, CAST(COUNT(DISTINCT o.subj) AS BIGINT) AS subjects_ FROM s_both o",
 "params": 21,
 "columns": [
  "rows",
  "subjects"
 ],
 "inlined": []
}